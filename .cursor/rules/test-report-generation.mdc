---
alwaysApply: false
description: viaim迭代版本测试报告生成规范（基于缺陷Excel + 功能点文档自动生成完整报告）
---
# 测试报告生成规范

本规则用于在迭代测试完成后，基于缺陷 Excel 表格和功能点简约描述，自动生成完整版测试报告 Markdown 文档。

详细操作流程见 skill：[test-report-generation/SKILL.md](mdc:.cursor/skills/test-report-generation/SKILL.md)

## 触发时机
- 用户明确要求"生成测试报告""写测试报告""输出本版测试报告"时触发。
- 通常在测试执行完毕、功能点文档已生成后执行。

## 所需输入（三项缺一不可）
1. **缺陷 Excel 文件**：格式同 `iFLYBUDS--xxxx-xx-xx.xlsx`，需包含 `严重程度` 字段
2. **功能点简约描述**：由 `@test-report-summary.mdc` 生成的 `xxx简约版测试点.md`
3. **版本基本信息**：版本号（如 V6.4.0）、测试周期（开始日期 - 结束日期）

## Excel 解析规则（已确认，不可修改）

### 字段映射
| Excel 字段 | 用途 |
|---|---|
| `标题` | 推断所属平台（取第一个【】标签） |
| `状态` | 推断闭环状态 |
| `优先级` | 统计各端优先级分布 |
| `严重程度` | 统计严重等级分布（格式：`1-致命`/`2-严重`/`3-一般`/`4-轻微`） |
| `工作项类型` | 过滤，仅计入 `缺陷` / `缺陷（产品/设计）` |
| `迭代` | 可用于过滤当前版本条目 |

### 平台识别规则
- 只取标题**第一个【】标签**判断平台，正文中出现的平台关键词忽略
- `【Android-xxx】` / `【现网Android-xxx】` → Android 端
- `【iOS-xxx】` / `【现网iOS-xxx】` / `【IOS-xxx】` / `【IOS-Vx.x.x】` → iOS 端
- `【服务端-xxx】` → 服务端
- `【现网xxx】` 前缀：**保留，合并计入总数**

### 状态映射规则
| Excel 状态值 | 报告状态 |
|---|---|
| `已关闭` | 已闭环 |
| `已关闭（未修复）` | 已闭环（计入总数） |
| `暂不修复` | 暂不修复 |
| `待修复` / `处理中` / `重新打开` | 待修复 |

## 报告结构（严格按 V6.3.0 格式）
1. 封面（版本号、公司、日期）
2. 版本记录（追加新版本行）
3. 引言 + 参与人员
4. 测试版本信息
5. 测试结论
   - 整体质量评估
   - 缺陷概览（总数、各端分布、遗留暂不修复数）
   - 风险评估（遗留问题说明）
6. 测试详情
   - 目标质量要求（固定表格，全部"通过"）
   - 测试范围（功能点评估表，插入功能点简约描述）
   - 缺陷分析（严重程度×状态分布表）
   - 缺陷各端占比（各端×优先级分布表）
   - 已知问题列表（暂不修复问题清单）

## 输出规范
- 输出完整 Markdown 格式，文件保存至 `xxx版本/测试报告_国内viaim_Vx.x.x.md`
- 所有统计数字必须从 Excel 解析后得出，不得估算
- 功能点评估列直接插入简约版测试点内容，结果列统一填"通过"
- 遗留问题说明中：非【严重】缺陷需在风险评估中明确标注
